export enum FirebaseAuthErrorCodes {
  WRONG_PASSWORD = 'auth/wrong-password',
  TOO_MANY_REQUESTS = 'auth/too-many-requests',
}

export enum FirebaseDatabaseErrorCodes {
  PERMISSION_DENIED = '?',
}
